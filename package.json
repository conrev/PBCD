{
  "name": "PBCD",
  "version": "1.0.0",
  "description": "Automated Method for Generating Secondary Motion using Position Based Dynamics",
  "source": "src/index.html",
  "author": "conrev",
  "license": "MIT",
  "private": false,
  "dependencies": {
    "parcel": "^2.6.0",
    "three": "^0.140.2"
  },
   "scripts": {
    "clean-docs": "rm -rf docs && mkdir docs",
    "copy-assets": "cp -r assets/* docs",
    "start": "yarn run clean-docs && yarn run copy-assets && parcel",
    "build-gh-pages": "yarn run clean-docs && yarn run copy-assets && parcel build src/index.html --public-url ./ --dist-dir docs",
    "playground" :"yarn run clean-docs && yarn run copy-assets && parcel src/fbxloader.html",
    "build": "parcel build"
  }
}
